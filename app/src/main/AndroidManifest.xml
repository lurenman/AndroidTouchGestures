<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lurenman.androidtouchgestures">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GestureDetectorActivity" />
        <activity android:name=".activity.VelocityTrackerActivity" />
        <activity android:name=".activity.MultiTouchActivity" />
        <activity android:name=".activity.ViewDragHelperActivity" />
        <activity android:name=".activity.ScrollMainActivity" />
        <activity android:name=".activity.ScrollActivity" />
        <activity android:name=".activity.ScrollerActivity" />
        <activity android:name=".activity.ScaleGestureDetectorActivity" />
    </application>

</manifest>